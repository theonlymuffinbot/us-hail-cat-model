# CONUS Hail Cat Model — Python dependencies
# Install with: pip install -r requirements.txt
# Python 3.9+ required

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Geospatial
rasterio>=1.3
xarray>=2023.1
regionmask>=0.11

# L-moments (used in CDF fitting, stage 10)
lmoments3>=1.0.4

# Parquet I/O (stochastic catalog, stage 14)
pyarrow>=12.0

# Visualisation (pipeline stages + render scripts)
matplotlib>=3.7
cartopy>=0.22
